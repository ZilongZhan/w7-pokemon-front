import "./Header.css";

const Header: React.FC = () => {
  return (
    <header className="main-header">
      <h1 className="app-title">PokéManager</h1>
    </header>
  );
};

export default Header;
